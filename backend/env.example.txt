# Server Configuration
PORT=3001

# Database Connection
# Replace with your PostgreSQL connection string (NO REAL SECRETS IN REPO)
# Format examples:
# - Local:  postgresql://postgres:password@localhost:5432/cybernauts_db?schema=public
# - Cloud:  postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Environment
NODE_ENV=development

# Example DATABASE_URL formats:
# PostgreSQL (local): postgresql://postgres:password@localhost:5432/cybernauts_db?schema=public
# PostgreSQL (cloud): postgresql://user:pass@host.example.com:5432/dbname?schema=public

